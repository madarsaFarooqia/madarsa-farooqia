/**
 * Mock API layer — delete this folder when the backend is integrated.
 * @see ./README.txt
 */
export { isMockApiEnabled, mockUserRole, mockDelay } from './config';
export { resetMockStores } from './state';
